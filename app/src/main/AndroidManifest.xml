<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nora"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SetHostActivity"
            android:exported="false"
            android:label="@string/title_activity_set_host"
            android:theme="@style/Theme.Nora.NoActionBar" />
        <activity
            android:name=".RegistroUsuario"
            android:parentActivityName=".MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.emanoxxxpc.nora.MainActivity" />
        </activity>
        <activity
            android:name=".CatalogoCategoriasActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CategoriaDeSonidoActivity"
            android:label="@string/title_activity_CategoriaSonido"
            android:parentActivityName=".MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.emanoxxxpc.nora.MainActivity" />
        </activity>
    </application>

</manifest>